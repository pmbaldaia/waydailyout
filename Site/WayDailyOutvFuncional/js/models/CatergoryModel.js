export default class CategoryModel {

    constructor(name, categories, photo, description) {
        this.name = name,
        //this.categories = categories,
        this.model = model,
        this.photo = photo,
        this.description = description
    }
}

